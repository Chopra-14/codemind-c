#include <stdio.h>
#include<math.h>
int main() {
        int t;
        scanf("%d",&t);
        int i;
        for(i=0;i<t;i++){
            int x,y,d;
            scanf("%d%d%d",&x,&y,&d);
            if(abs(x-y)<=d) printf("yes\n");
            else printf("no\n");
        }
}
