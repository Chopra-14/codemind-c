#include <stdio.h>
int main() {
    int t,a;
    scanf("%d", &t);
    for(int i=0;i<t;i++){
        int x, y, z;
        scanf("%d %d %d", &x, &y, &z);
        a=((x*5)+(y*10))/z;
        printf("%d\n",a);
    }
}
