#include <stdio.h>
int main() {
	int t,n;
	scanf("%d", &t);
	while(t--){
	    scanf("%d", &n);
	    int a[n],b=0;
	    for(int i=0;i<n;i++){
	        scanf("%d", &a[i]);
	        b^=a[i];
	    }
	    printf("%d\n",b);
	}
}
