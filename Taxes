#include <stdio.h>
int main()
{
    int n,i;
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        int x;
        scanf("%d",&x);
        if(x>100)  printf("%d\n",x-10);
        else printf("%d\n",x);
    }
}
